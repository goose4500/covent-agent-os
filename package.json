{
  "name": "covent-agent-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private Covent AI automation operating layer for Slack, Pi, skills, agents, and bounded tools.",
  "workspaces": [
    "apps/pi-mom",
    "apps/pi-preview"
  ],
  "scripts": {
    "check": "bun run typecheck && bun run check:pi-mom && bun run check:pi-preview",
    "doctor": "bun run doctor:pi-mom",
    "dev:pi-mom": "cd apps/pi-mom && bun run start",
    "dev:pi-preview": "cd apps/pi-preview && bun run start",
    "doctor:pi-mom": "cd apps/pi-mom && bun run doctor",
    "check:pi-mom": "cd apps/pi-mom && bun run check",
    "check:pi-preview": "cd apps/pi-preview && bun run check",
    "typecheck": "tsc --noEmit",
    "install:pi": "bash scripts/install-local.sh"
  },
  "pi": {
    "extensions": [
      "./extensions/git-checkpoint.ts"
    ],
    "skills": ["./skills"],
    "agents": ["./agents"]
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@types/node": "^22.15.3",
    "typebox": "latest",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=22"
  }
}
