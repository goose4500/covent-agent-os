{
  "eval_id": 1,
  "eval_name": "ci-service-token-deploy",
  "prompt": "We want to deploy a Slack app from CI. The service token is stored in an env var named SLACK_SERVICE_TOKEN. What Slack CLI command should the workflow use, and what should I be careful about?",
  "assertions": [
    "Includes CI command using slack deploy -s --token \"$SLACK_SERVICE_TOKEN\" or equivalent env-var token usage",
    "States service tokens are secrets and should not be printed, committed, or logged",
    "Mentions confirming target workspace/team/app/environment before production deployment",
    "Mentions --skip-update/-s or skip update checks for CI stability"
  ]
}